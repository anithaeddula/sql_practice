-- 1. Display every customer name in uppercase--
select upper(email)from customers;
-- 2. Write a query to find the length of the string 'MySQL'--
select length('mysql');
-- 3. Write a query to remove leading and trailing spaces from ' Hello MySQL '--
select trim(' Hello MySQL ');
-- 4. Write a query to extract the first 5 characters from 'Database'--
select substring('database',1,4);
-- 5.Write a query to concatenate 'Algonex' and 'Crew'--
select concat('Algonex','','Crew');
-- 6. Write a query to replace 'Java' with 'Python' in a example string--
select replace('im  learning java','java','python');
-- 7. Write a query to display an employee's first name and last name as one column--
select concat(first_name,' ',last_name) as full_name from table_name;
-- 8. Write a query to display the first 3 characters of every employee's name.--
select substring(first_name,1,3) as first_3_characters from customers;
-- 9.Write a query to round 125.6789 to 2 decimal places--
select round(125.6789,2);
-- 10.Write a query to find the ceiling value of 45.23--
select ceil(45.23);
-- 11.Write a query to find the floor value of 45.89--
select floor(45.89);
-- Write a query to find the absolute value of -250--
select abs(-250);
-- Write a query to find the remainder when 25 is divided by 4--
select mod(25,4);
-- 14. Write a query to calculate 5²--
select power(5,2);
-- 15.What is the difference between NOW() and CURDATE(), DATEDIFF() and CURRENT_DATE()--
-- now()-returns  current date and time
select now();
-- curdate()-returns current data.
select curdate();
-- datediff()-return diff in days.
select datediff('2026-01-27','2026-01-01');
-- 16. Write a query to extract the year from an employee's joining date--
select year(joining_date) as joining_year from employee;
-- 17.Write a query to find the number of years an employee has worked--
select timestamp(year,joining_date,curdate())as joining_year from table_name;
-- 18.Write a query to find employees who joined within the last 6 months--
select date_sub(curdate(),interval 6 month);
-- 19.Write a query to format a date as DD-MM-YYYY--
select date_format(date_format,'%d-%m-%Y');
/*20. Write a query that displays:High if salary > 80,000
Medium if salary is between 50,000 and 80,000
Low otherwise*/
select product_name,price ,
   case
      when salary>80000 then 'HIGH'
      when salary between 50000 and 80000 then 'MEDIUM'
      else 'LOW'
   end as category
From employee;
-- aggregate functions--
-- count(*)---
select count(*) from books;
-- count(column)---
select count(author) from books;
-- count(distinct)---
select count(distinct(book_id)) from books;
-- sum--
select sum(salary) as revenue from employee;
-- avg--
select avg(salary) as avg_sala from employee;
-- min--
select min(salary) from employee;
-- max--
select max(salary) from employee;
